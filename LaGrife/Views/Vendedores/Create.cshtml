@model LaGrife.Models.ViewModels.VendedorFormViewModel

@{
    ViewData["Title"] = "Cadastrar vendedor";
}

<h1 class="text-center mb-4">@ViewData["Title"]</h1>

<hr />
<div class="row justify-content-center">
    <div class="col-md-6">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <label asp-for="Vendedores.Nome" class="form-label fw-bold"></label>
                <input asp-for="Vendedores.Nome" class="form-control" placeholder="Digite o nome do vendedor" />
                <span asp-validation-for="Vendedores.Nome" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Vendedores.Email" class="form-label fw-bold"></label>
                <input asp-for="Vendedores.Email" class="form-control" placeholder="Digite o e-mail do vendedor" />
                <span asp-validation-for="Vendedores.Email" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Vendedores.Aniversario" class="form-label fw-bold"></label>
                <input asp-for="Vendedores.Aniversario" type="date" class="form-control" />
                <span asp-validation-for="Vendedores.Aniversario" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Vendedores.LojaId" class="control-label"></label>
                <select asp-for="Vendedores.LojaId" asp-items="@(new SelectList(Model.Lojas,"Id","Local"))" class="form-control"></select>
            </div>

            <div class="form-group text-center">
                <input type="submit" value="Cadastrar" class="btn btn-success btn-lg" />
                <a asp-action="Index" class="btn btn-secondary btn-lg">Voltar para a lista</a>
            </div>
        </form>
    </div>
</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}